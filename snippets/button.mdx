export const Button = ({
  label,
  href,
}) => {
  const handleClick = () => {
    if (href) {
      if (href.startsWith('http')) {
        window.open(href, '_blank');
      } else {
        window.location.href = href;
      }
    }
  };

  return (
    <button 
      className="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md font-semibold transition-colors duration-200"
      onClick={handleClick}
    >
      {label}
    </button>
  );
}

export const ButtonSecondary = ({
  label,
  href,
}) => {
  const handleClick = () => {
    if (href) {
      if (href.startsWith('http')) {
        window.open(href, '_blank');
      } else {
        window.location.href = href;
      }
    }
  };

  return (
    <button 
      className="bg-white dark:bg-gray-950  hover:bg-gray-100 dark:hover:bg-gray-800 text-gray-800 dark:text-gray-200 border border-gray-200 dark:border-gray-600 px-4 py-2 rounded-md font-semibold transition-colors duration-200"
      onClick={handleClick}
    >
      {label}
    </button>
  );
}
